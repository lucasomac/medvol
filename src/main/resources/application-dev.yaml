spring:
  datasource:
    url: ${DB_HOST_DEV}
    username: ${DB_USER_DEV}
    password: ${DB_PASS_DEV}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: postgresql
    generate-ddl: true
    properties:
      hibernate:
        format_sql: true